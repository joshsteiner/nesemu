PPUDATA should read/write from vram
PPU::2007 should read/write from vram not ram
use propper ISO C++ style (naming, indents)
    constants/enums : ConstValue -> const_value
    classes         : ClassName -> Class_name
order public/private members
remove all code from class declarations